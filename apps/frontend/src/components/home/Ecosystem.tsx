import React from 'react';
import { Box, Typography } from '@mui/material';

const Ecosystem: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h2">Ecosystem</Typography>
    </Box>
  );
};

export default Ecosystem;
