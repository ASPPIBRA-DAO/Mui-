import React from 'react';
import { Box, Typography } from '@mui/material';

const FAQ: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h2">FAQ</Typography>
    </Box>
  );
};

export default FAQ;
