import React from 'react';
import { Box, Typography } from '@mui/material';

const FinalCTA: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h2">FinalCTA</Typography>
    </Box>
  );
};

export default FinalCTA;
