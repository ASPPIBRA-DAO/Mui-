import React from 'react';
import { Box, Typography } from '@mui/material';

const Team: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h2">Team</Typography>
    </Box>
  );
};

export default Team;
