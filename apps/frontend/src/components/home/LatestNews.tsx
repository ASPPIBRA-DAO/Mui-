import React from 'react';
import { Box, Typography } from '@mui/material';

const LatestNews: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h2">LatestNews</Typography>
    </Box>
  );
};

export default LatestNews;
