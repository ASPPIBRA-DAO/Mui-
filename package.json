{
  "name": "root-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "deploy": "pnpm -r deploy"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.51.0",
    "mongoose": "^8.20.1",
    "@typegoose/typegoose": "^12.20.0"
  }
}
